{
  "mcpServers": {
    "engram": {
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}", "engram-mcp"],
      "env": {
        "ENGRAM_STORE_DIR": "${CLAUDE_PLUGIN_DATA}/store",
        "ENGRAM_TRANSPORT": "stdio"
      }
    }
  }
}
