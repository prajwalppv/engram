{
  "mcpServers": {
    "engram": {
      "command": "bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/engram-launch", "mcp"],
      "env": {
        "ENGRAM_TRANSPORT": "stdio"
      }
    }
  }
}
