---
description: Save a durable memory for future sessions
---
Save a memory with the `memory_save` tool capturing: $ARGUMENTS

Choose a `type` appropriate to my inferred role (call `role_status` if unsure), a concise unique `title`, and set `repo` to the current project. Confirm what you stored.
